server:
  port: 8888
spring:
  cloud:
    config:
      server:
        git:
          uri: https://github.com/tree0122/distributed
          search-paths: config-repo/licensingservice
        native:
          search-locations: classpath:config/licensingservice
  application:
    name: configserver
  profiles:
    active: git, native